ext{
  versionCode = 1000
  versionName = "1.0.0"

  applicationId = "com.zjf.edgeai"

  android = [
      versionCode: versionCode,
      versionName: versionName,
      applicationId: applicationId,
      compileSdkVersion: 34,
      minSdkVersion: 31,
      targetSdkVersion: 34]

  coreKtxVersion = "1.13.1"
  appcompatVersion = "1.7.0"
  materialVersion = "1.12.0"
  activityKtxVersion = "1.9.3"
  fragmentKtxVersion = "1.8.5"
  lifecycleVersion = "2.8.7"
  recyclerViewVersion = "1.3.2"
  viewPager2Version = "1.1.0"
  // LiteRT-LM 0.15.0 invokes SendChannel.close$default from its completion
  // callback. That bridge is provided by coroutines 1.11.0, despite the
  // LiteRT-LM POM declaring 1.9.0.
  coroutinesVersion = "1.11.0"
  liteRtLmVersion = "0.15.0"
}
