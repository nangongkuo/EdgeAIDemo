## 1. 多语言运行时策略

- [x] 1.1 在 `edge-ai` 中添加项目自有的中文优先语言策略和确定性采样配置。
- [x] 1.2 将该策略用于每一条 LiteRT-LM Conversation 创建路径，不改变原生生命周期或后端回退行为。
- [x] 1.3 移除当前轮次 `wrapPrompt` 转换，使 `Conversation.sendMessageAsync()` 仅接收原始的非空用户提示词；保留 Conversation 级系统指令和采样配置。

## 2. 对话交互与说明

- [x] 2.1 经由 ViewModel 原样转发每条非空用户提示词，并在对话历史中原样保留它。
- [x] 2.2 在对话页添加支持中文/英文/混合输入及默认简体中文回答的说明。

## 3. 自动化回归覆盖

- [x] 3.1 添加语言策略、原始混合语言提示词转发、CJK 流式更新和重置行为的单元测试。
- [x] 3.2 添加使用内置模型的 ARM64 真机专用离线多语言推理验收测试。
- [x] 3.3 添加 LiteRT-LM 精确用户轮次负载转发及防止代表性中文问题被规范化回显为助手响应的回归。必需项：`BI-1` 与 `BI-2`。

## 4. 验证与交付门槛

- [x] 4.1 运行 OpenSpec 严格验证、单元测试、lint 和调试/APK 构建检查。
- [ ] 4.2 在已连接 Android 12+ ARM64 设备上运行固定的中文、英文、混合语言、字面内容、多轮和重置语料；若模型未满足语言契约，此任务必须保持未完成。
- [x] 4.3 在不声明 `INTERNET` 权限的应用上，于连接的 Android 12+ ARM64 设备运行直接回答回归语料：能力问题、英文问题、混合语言问题。捕获截图、UI 层级和相关日志；要求出现非回显的中文能力回答。必需项：`BI-4`。该语料验证提示词路由和响应生成，而非模型的算术或事实准确性。
